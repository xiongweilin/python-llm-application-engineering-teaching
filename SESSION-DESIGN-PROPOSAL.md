# 学习会话重新设计提案

状态：历史设计提案（2026-07-29），不再是当前课程规则或路线。

本页保留当时的五个独立会话方案，用于说明设计演变，不应据此判断当前会话数量、名称、任务粒度或未来位置。当前唯一有效规范与会话序列由 `SESSION-PAGE-CONTRACT.md` 持有；完整路线与最终能力分别由 `reference/0002-course-progress.html` 和 `FINAL-CAPABILITY-CONTRACT.md` 持有。

采用后的固定原则：一次只显示一个步骤；抽象底层逻辑与具体 Python、数学、张量或系统细节分别解释；三个相关任务完成后统一与老师互动一次；每个正式会话页面下方提供本会话中文术语表。首个正式实现为 `lessons/0010-state-and-permitted-actions.html`，长期规范由 `SESSION-PAGE-CONTRACT.md` 持有；完整路线与最终能力分别由 `reference/0002-course-progress.html` 和 `FINAL-CAPABILITY-CONTRACT.md` 持有。

## 要解决的问题

当前练习把一道小题同时当成知识讲解、编码练习、迁移考核和师生互动单位，产生四个问题：

- 一页内概念很多，但关键概念解释仍不够完整；
- 每题都要整理并提交，打断连续思考；
- 六题并列、侧栏和多块输入同时可见，容易分散注意；
- 题目彼此跨度较大，完成一题后没有形成一段完整能力再进入下一题。

## 新的五级单位

| 单位 | 作用 | 大致尺度 |
|---|---|---|
| 课程 | 获得两套项目的代码所有权 | 整体路线 |
| 学习环 | 围绕真实项目形成一组能力 | 若干会话 |
| 学习会话 | 只建立一个中心能力 | 30–45 分钟 |
| 任务 | 从不同角度练同一能力 | 每会话 3 个 |
| 步骤 | 阅读、预测、编写、运行或校正 | 3–12 分钟 |

“一个单元只教一件事”保留，但单元不再等于一道题。一个会话用三种任务建立同一能力，并只在会话结束后提交一次。

三项任务不是沿用原来的题数，而是由证据需要决定：第一项证明能辨认，第二项证明能实现，第三项证明条件变化后仍能迁移。只有两项时容易把“照着写对”误认为理解；超过三项则优先拆成下一会话，不让一次专注时间无限延长。

## 一个学习会话的固定结构

```text
1. 定向：今天学什么、为什么、完成后能做什么
2. 建模：生活化结构 → Python 对象 → 当前接口细节
3. 任务一·辨认：读例子并预测，不写完整代码
4. 任务二·编写：在支架下完成核心代码并自动检查
5. 任务三·迁移：改变条件、找反例或诊断故障
6. 汇总：用自己的话连接三项任务
7. 一次提交：统一复制给老师，获得一次综合反馈
```

每个任务仍有网页即时反馈。只有以下情况中途找老师：概念无法理解、已经尝试两种做法仍无法前进，或检查结果与预测互相矛盾。

## 解释与引导怎样增加，但不增加干扰

- 页面第一屏只出现本次目标、预计时间和当前一步。
- 每个概念依次提供：一句中文定义、一个流程图、一个小例子、对应 Python 写法。
- 英文和对象模型放在“为什么 Python 这样表现”层，不抢在基本结构前面。
- 当前任务只显示一个主要按钮；其余任务折叠或隐藏。
- 提示分三级：方向提示 → 代码骨架 → 关键一行；默认只显示第一级。
- 页面底部始终显示“本会话结束后统一提交”，不再每题催促对话。
- 允许写“暂时不懂”，汇总时集中处理，不要求立刻解决每个旁支。

## 重新组织接下来的内容

第 1–2 题已经形成的学习记录保留，不要求重做。后续内容不直接搬运原题，而按能力重写为会话：

1. **状态与允许行动**：辨认状态/事件/转换，编写状态转换，迁移到取消与非法转换。
2. **失败、重试与停止**：区分暂时/永久失败，编写有限重试，诊断间隔和异常分类。
3. **操作身份与防重复**：区分内容与操作身份，实现单进程幂等，再用并发反例引出数据库唯一性。
4. **批处理与错误边界**：区分业务错误与系统故障，完成批处理，诊断错误吞噬。
5. **模块责任与调用链**：从业务需求预测入口/流程/规则，核对混合源码，完成一次小改动与测试。

每两个会话后的下一会话开头加入一次无答案间隔回忆。学习记录按会话中心能力生成，不按三道小题机械生成。

## 原型比较记录

- **A·专注隧道**：一次只显示一个步骤，最少干扰，但可能失去整体位置感。
- **B·教练工作台**：左侧始终显示路线，右侧处理当前材料，方向清楚但信息更多。
- **C·渐进讲义**：连续阅读，任务按需展开，解释最完整但页面较长。

原型不保存答案、不计入学习进度。选择结果已经吸收到正式会话；原型退出活动课程入口，不再承担教学进度。

## 采用标准

- 打开页面五分钟内，能够说清今天只学哪一个能力。
- 不需要教师解释页面怎么用。
- 当前视野只有一个明确的下一动作。
- 三个任务之间能感到是同一问题逐步加深，而不是三道散题。
- 完成三题后可以一次提交，提交内容足以判断理解、代码和迁移。
- 页面能够容纳“详细解释”，但次要信息不会持续占据注意。
