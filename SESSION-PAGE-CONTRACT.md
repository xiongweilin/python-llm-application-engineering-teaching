# 正式学习会话页面契约

状态：当前课程的唯一活动教学页面规范（2026-07-31）。

## 路线坐标

活动课程只使用以下层级表达位置：

```text
最终能力 → 阶段 → 学习环 → 正式会话 → 三个不同问题及各自步骤 → 本会话中文术语 → 可选参考卡
```

“第几课”和 HTML 文件编号只作为历史文件名，不再表示当前学习顺序。任何参考卡都必须写明自己服务于哪个正式会话，以及该会话当前是进行中、已完成还是未开始。

## 正式会话必须包含

1. 全课程统一最终目标的摘要，以及“阶段 → 学习环 → 正式会话”的当前位置；
2. 通常三个不同问题；每个问题各有一个中心能力、明确边界和独立通过证据；
3. A“专注隧道”式单步显示，一次只有一个当前动作；
4. 每个问题完整经过七步：定向、底层逻辑、具体编程或计算细节、预测、核心实现、条件变化后的迁移、问题小结；
5. 每个问题包含预测、核心实现、迁移三个任务，各有独立网页反馈；三个问题的正式会话共有九个任务；
6. 一次会话级综合提交，不按题打断对话；
7. 页面下方的“本会话中文术语”，只收录理解当前会话必须用到的词；
8. 指向总术语表、完整路线和最终能力契约的链接；
9. 本地保存、失败说明、来源和能力证据边界。

## 其他页面身份

- **综合复习会话**：把已有学习记录重新连接成完整判断链；采用单步、三个综合任务与局部术语，但不自动新增记录，也不推进正式会话进度。
- **已完成复习页**：对应已有学习记录，只供可选复习；不得再称为当前练习或要求重新提交。
- **候选参考页**：尚未进入或等待重写的材料；不得出现在当前行动入口，必须声明未来归属会话。
- **参考卡**：服务于某个正式会话的压缩材料；必须标注归属和状态，不能直接用旧课编号解释位置。

## 当前会话序列

当前序列属于**阶段 A · 环 1“可执行 Python 与可靠节点”**，不是整门课程：

1. 状态与允许行动（已完成的短会话，作为体量校准样本）
2. 重试、防重复与批处理（进行中，包含三个不同问题）
3. 模块、诊断与授权边界（未开始；会话开头先完成无答案间隔回忆，再依次学习三个问题：模块责任与调用链、traceback 与测试诊断、模型候选与已授权行动）

正式会话 2 结束后，在会话 3 前加入第一次无答案间隔回忆。

完整课程为四阶段、十七环；后续环级事实由 `reference/0002-course-progress.html` 唯一持有，最终能力由 `FINAL-CAPABILITY-CONTRACT.md` 唯一持有。
