# 正式学习会话页面契约

状态：当前课程的唯一活动教学页面规范（2026-08-18）。

## 路线坐标

活动课程只使用以下层级表达位置：

```text
唯一最终能力事实源 → 阶段 → 学习环 → 正式会话 → 主要完整工作单元 → 能力证据阶段 → 本会话中文术语 → 可选参考卡
```

文件编号和旧课编号只作为历史文件名，不能表示当前学习顺序。参考卡必须写明服务对象、所属正式会话以及当前状态。

## 核心设计：完整工作单元

阶段 A 的基本教学单位不是一个知识点、一个裸函数，也不是一行代码，而是一个：

> **完整、可观察、可验证的工程工作单元。**

工作单元可以是：

- 一个完整函数及其调用轨迹；
- 从失败测试、traceback 到源码定位和验证的完整诊断过程；
- 一条跨模块调用链及其责任判断；
- 一个从行为契约到局部代码修改、测试和回归的完整修复过程。

“完整程序”只是工作单元的一种，不要求每次都手写一个新程序。

## 正式会话必须包含

1. 全课程最终目标摘要，以及“阶段 → 学习环 → 正式会话”的当前位置；
2. 一个主要完整工作单元，以及它的现实背景、程序责任、观察证据和边界；
3. 单步聚焦界面：一次只突出一个当前动作，点击步骤后滚动到该动作的顶部，不把页面强制送回顶部；
4. 一个完整获得循环：

   ```text
   最小基础包
   → 完整范例与运行轨迹
   → 完整模仿
   → 完整重建
   → 受控变化
   → 新情境迁移
   → 稳定骨架与可变参数抽取
   ```

5. 三类能力证据。推荐把一个主要工作单元编排成三个任务阶段，而不是三个互不相关的问题：

   | 阶段 | 主要证据 | 不单独证明 |
   | --- | --- | --- |
   | 模式获得 | 能追踪完整结构，并在脚手架下重建 | 不能证明已能迁移 |
   | 受控变化 | 能在完整结构上只改变一个规则并验证稳定部分 | 不能证明能处理陌生结构 |
   | 迁移与组块化 | 能在新情境中识别稳定骨架、可变参数和边界 | 不能替代真实运行证据 |

6. 预测必须先于运行验证。每个工作单元至少覆盖代表性轨迹或证据：正常路径、关键失败路径、边界路径，以及一个未见条件；先预测，再运行，再比较；
7. 完整范例中的可见 Python 代码逐行旁注。旁注解释对象、输入/输出、控制流、副作用、放置原因及与相邻代码的关系，不只重复语法名称；
8. 再激活支持和帮助记录。语法检索失败不能被误判为结构能力缺失，模仿也不能被升级为掌握；
9. 一次会话级统一提交，记录各阶段证据、帮助类型、运行结果、仍不确定之处和实际投入时间；每个可操作小题只保留一个唯一未知量；
10. 页面底部的“本会话中文术语”，以及总术语表、完整路线、最终能力契约的链接；
11. 本地保存、失败说明、来源和能力证据边界。自动检查只提供反馈，不替代解释、重建、诊断或迁移证据。

## 一个工作单元的教学循环

### 1. 最小基础包

只讲完成当前工作单元所必需的对象、过程、语法和接口。先用普通中文说明现实事件，再建立程序模型，最后给 Python 写法。

数量和状态变量使用：

```text
含义 → 类型/单位 → 取值 → 变化时机 → 改变者 → 影响步骤
```

程序结构使用：

```text
它做什么 → 什么时候执行 → 控制哪段代码 → 执行后程序去哪里
```

函数或接口使用：

```text
输入 → 输出 → 副作用 → 可能失败 → 调用条件
```

不能把同一张属性表机械套给变量、语法和接口。

### 2. 完整范例与运行轨迹

先展示能够运行的完整范例，再沿至少两个具体输入追踪：

```text
输入 → 当前状态 → 分支/调用 → 输出或副作用 → 下一步
```

学习者先作预测，再运行范例，比较预测和证据。范例中的 `pass`、异常类型、函数调用和参数都必须在第一次出现时解释。

### 3. 完整模仿

学习者可以看着参考代码完整输入并运行。推荐采用：

```text
看一小段 → 说它负责什么 → 自己输入 → 运行局部检查 → 继续下一段
```

完整模仿用于建立顺序、缩进、语法形状和局部连接，不能作为独立能力证据。输入完成后必须紧接一次完整 trace 或行为解释。

### 4. 完整重建

撤掉完整参考，但允许查询语法。帮助按以下层级逐步减少：

```text
完整参考代码
→ 关键代码行
→ 代码块标题
→ 自然语言步骤
→ 函数签名与测试
→ 只有行为契约
```

学习者在某一层失败时回退一级，而不是直接判定为“不会 Python”。完整重建是阶段 A 的核心证据，因为它检验能否把局部知识重新组合成一个整体。

### 5. 受控变化

在学习者自己的完整代码上只改变一个明确维度，例如固定等待改为指数退避、上限从 3 改为 4，或把一条边界从“超过”改为“达到”。必须先说明：

```text
要改变的规则 → 预期改变的行为 → 必须保持的行为 → 运行证据
```

不要同时改变接口、异常模型、返回值和等待策略。

### 6. 新情境迁移

迁移必须标注距离：

```text
Level 1：名称和业务情境变化，结构相同
Level 2：参数或常量变化
Level 3：一个边界规则变化
Level 4：一个结构维度变化
Level 5：在真实项目中找到同一模式
```

早期阶段从 Level 1–2 开始，逐步进入 Level 4–5。只改函数名不应被记录为高阶迁移。

### 7. 稳定骨架与可变参数抽取

最后同时回答两件事：

```text
哪些步骤跨情境保持不变？
哪些规则、常量、错误类型、接口或副作用可以改变？
```

程序模式应记录成：

```text
稳定骨架 + 可配置变化点
```

而不是一份必须逐字复刻的模板。

## 阶段 A 的证据坡度

阶段 A 不把“会写 Python”当作单一能力。每个工作单元分别记录：

```text
R = Recognize：识别完整模式
T = Trace：沿输入、状态、控制流和副作用追踪
P = Produce：从骨架或契约重建
M = Modify：在稳定结构上受控修改
D = Diagnose：用测试、traceback 或运行证据定位问题
X = Transfer：迁移到新情境或真实项目
```

典型证据记录可以是：

```text
Retry：R ✓  T ✓  P △  M ✓  D △  X △
```

不能用“页面完成”“自动检查通过”或“同例照抄”替代这些维度。

## 阶段 A 注意事项

正式页面和新题目必须遵守以下审查表：

1. 学习者是否知道现实上发生了什么？
2. 每个量是否有唯一含义、单位或类型？
3. 事件顺序是否先于代码操作建立？
4. 当前真正未知的是什么？是否只有一个主要未知量？
5. 是否明确哪些代码属于既有程序、哪些地方属于练习操作？
6. 是否区分“是否继续/是否产生副作用”和“如果产生，值是多少”？
7. 是否先预测、再运行、再比较？
8. 是否已经把答案写在题面或选项里，造成回显？
9. 答错后能否判断是问题逻辑、程序结构、Python 表达还是运行证据出了问题？
10. 正确后是否能在近邻新情境中复用稳定骨架？

任何一项回答不了，题目仍处于设计阶段，不能靠增加术语或注释直接上线。

## 术语来源协议

第一次出现且可能陌生的词必须标明来源，并先解释现象：

- `[普通中文]`：日常表达；
- `[程序概念]`：如控制流，指执行、跳过、分支、返回和中断的顺序；
- `[Python 语法]`：如 `except`，指语言构造及其写法；
- `[代码变量]`：如 `attempt`，是代码作者给数据取的名字；
- `[标准技术术语]`：如幂等、唯一约束、异常传播；
- `[课程说法]`：本页为了方便说明临时采用的词，不要求当作标准术语记忆。

如果普通中文已经足够，不制造新的压缩词。

## 帮助与编辑范围

阶段 A 至少提供五类帮助：

1. 我看不懂题目让我做什么；
2. 我不知道要观察或修改哪段代码；
3. 我知道位置，但不知道 Python 怎么写；
4. 我知道写法，但不知道为什么这样改；
5. 查看参考答案。

编辑范围按以下坡度撤除：

```text
填一个槽位 → 改一行 → 改一个代码块 → 函数内定位 → 文件内定位 → 项目内定位
```

既有程序默认只读展示；需要修改时，页面单独标出 editable locus，并明确其余代码保持不变。点击步骤后使用活动内容的顶部定位，例如 `scrollIntoView({block: "start"})`；不得无条件执行 `window.scrollTo({top: 0})`。

## 会话粒度与间隔回忆

一个正式会话原则上围绕一个主要完整工作单元，目标体量约 30–45 分钟。会话内的“三个任务”是三类证据阶段，不是三个互不相关的问题：

1. 模式获得；
2. 受控变化；
3. 迁移与组块化。

如果三个模式都需要完整循环，应拆成多个正式会话，而不是把每个模式压缩成两三个局部小题。每 2–3 个正式会话安排一次无答案间隔回忆，检查已有工作单元是否仍能被压缩成控制流骨架或责任地图。

## 当前会话序列

当前序列属于**阶段 A · 环 1“可执行 Python 与可靠节点”**：

1. 状态与允许行动（已完成；作为体量校准样本）；
2. **受控重试：完整工作单元（进行中）**：同一个 retry 模式经历模式获得、受控变化、迁移与组块化；
3. 模块、诊断与授权边界（未开始）：开头先做无答案间隔回忆，再学习模块责任与调用链、traceback 与测试诊断、模型候选与已授权行动。

防重复与批处理的既有学习记录保留为历史证据，后续以各自完整工作单元重新学习，不再为了凑足一次会话而与 retry 并列塞入同一页面。

完整课程为四阶段、十七环；后续环级事实由 `reference/0002-course-progress.html` 唯一持有，最终能力由 `FINAL-CAPABILITY-CONTRACT.md` 唯一持有。
