# 正式学习会话页面契约

状态：当前课程的唯一活动教学页面规范（2026-08-18）。

## 路线坐标

活动课程只使用以下层级表达位置：

```text
唯一最终能力事实源 → 阶段 → 学习环 → 正式会话 → 三个不同问题及各自步骤 → 本会话中文术语 → 可选参考卡
```

“第几课”和 HTML 文件编号只作为历史文件名，不再表示当前学习顺序。任何参考卡都必须写明自己服务于哪个正式会话，以及该会话当前是进行中、已完成还是未开始。

## 正式会话必须包含

1. 全课程统一最终目标的摘要，以及“阶段 → 学习环 → 正式会话”的当前位置；
2. 通常三个不同问题；每个问题各有一个中心能力、明确边界和独立通过证据；
3. A“专注隧道”式单步显示，一次只有一个当前动作；
4. 每个问题完整经过七步：定向、教师示范与底层逻辑、具体编程或计算细节、预测、引导式局部补全、一个有界条件变化、问题小结；
5. 每个问题包含预测、引导式局部补全、有界变化三个任务，各有独立网页反馈；三个问题的正式会话共有九个任务。任务必须给出近完整脚手架、示例输入/输出或明确的改动位置，不得把第一次接触的能力写成空白设计题；
6. 一次会话级综合提交，不按题打断对话；
7. 页面下方的“本会话中文术语”，只收录理解当前会话必须用到的词；
8. 指向总术语表、完整路线和唯一最终能力事实源的链接；
9. 本地保存、失败说明、来源和能力证据边界；帮助使用必须与结构能力分开记录。

## 教学坡度

正式会话的结构相似，不代表每个阶段都要求同样程度的独立思考。阶段 A 的主要目标是取得真实 Python 代码的所有权，因此新概念遵循：

```text
教师示范 → 共同追踪输入/输出 → 局部补全 → 运行检查 → 一个有界变化 → 解释稳定结构
```

阶段 A 不要求学习者从空白页自行设计重试、幂等、并发或批处理方案。需要比较多个方案、改变损失和约束、建立模型、处理反例或进行开放式迁移的训练，放到阶段 B–D，或在阶段 A 后续关口明确标记为独立任务。自动检查只验证局部行为，不把“能猜中或能照抄”当作能力记录。

阶段 A 的可见 Python 示范和起始代码应逐行旁注。旁注优先解释这一行读取或产生什么、怎样改变控制流或副作用、为什么放在当前位置，以及它与上一行/下一行的关系；不只重复 `for`、`if`、`return` 等语法名称。学习者应能沿着代码和旁注完成第一次阅读，不必在页面之间来回寻找解释。

## 再激活型学习者支持

阶段 A 默认面向“以前学过 Python，但具体语法暂时调用不出来”的学习者。课程不把语法检索失败误判为结构能力缺失，也不把语法卡的使用自动记为已掌握。正式会话应同时提供两种路径：

- **建议路径**：约 12 个高价值动作，优先完成地图、三个问题的预测/局部修正/有界变化和统一提交；
- **完整讲解**：保留七步闭环中的定向、Python 细节和问题小结，供需要更多解释或复盘时展开。

两种路径共享同一组任务、检查器和提交证据；建议路径只是减少页面导航摩擦，不删除正式能力证据。

帮助按具体语法点递减，而不是按整节课一次开关。提示顺序为：

```text
语法卡 → 结构追问 → 具体定位 → 参考修改
```

逐行旁注可以解释对象、输入/输出、控制流、副作用和放置原因，但默认任务旁注不得直接给出语义答案。具体的 `if`/`elif`、`>`/`>=`、异常类型或等待表达式，应放在折叠提示或检查后的反馈中。语法正确而结构判断错误，属于需要追问的真实缺口；结构判断正确而语法暂忘，允许查询后继续。

学习证据至少区分：结构追踪、局部修改、条件迁移、语法帮助级别和学习者自己的解释。阶段 A 的毕业标准是能在帮助减少后稳定追踪和修改代码，而不是完全不查资料。

每个“局部修正”任务还必须显式标出可编辑位置（editable locus）：

- 既有程序代码使用只读展示，不让整段代码看起来都像待填空白；
- 页面单独提供一个输入框、选择器或短表达式编辑框，只承载本题要改变的值或片段；
- 题目明确说明哪些代码保持不变，并在代码旁直接标出唯一修改位置；
- 第一次出现 `pass` 时必须解释：它是合法 Python 语句，表示故意不执行任何动作，不等于 TODO 或练习占位符；
- 检查器可以把局部输入重新嵌入完整源码，但学习者不需要在完整源码中自行猜测题目位置。

有界变化任务不能把自然语言要求直接跳接到“修改表达式”。必须先把变量、输入、输出或状态变化做成可观察的对应关系/映射（例如 `attempt=1,2,3` 对应等待序列 `1,2,4`），并单独说明边界条件何时阻止副作用。之后才显示唯一表达式位置，并解释“表达式”是括号内会计算出参数值的代码。注释行、变量名和真正待修改的表达式必须在视觉和语言上分开。

## 阶段 A 翻译层

阶段 A 的统一教学管线不是“理解题目 → 修改完整 Python”，而是：

```text
现实含义 → 程序责任 → 代码位置 → Python 写法 → 学生操作 → 运行验证
```

每个早期代码任务必须显式提供这六层：

1. **现实含义**：先用普通话说明系统中发生了什么，不先抛出术语；
2. **程序责任**：指出哪一段程序负责这个效果，例如“这一分支只处理可能恢复的失败”；
3. **代码位置**：高亮或单独展示本题要观察/修改的行，明确其余代码保持不变；
4. **Python 写法**：第一次出现的函数调用、参数、变量、`if`、`for`、`except`、`raise`、`pass`、列表、字典等写法默认解释；
5. **学生操作**：只要求当前层级的动作，从填一个槽位开始，不让学习者同时定位、设计和回忆语法；
6. **运行验证**：说明检查器观察什么，以及它不能证明什么。

### 术语来源协议

第一次出现且可能陌生的词必须标明来源，并先用普通话解释现象：

- `[普通中文]`：日常表达，不要求作为技术术语记忆；
- `[程序概念]`：如控制流，指程序实际执行、跳过、分支、返回和中断的顺序；
- `[Python 语法]`：如 `except`，指语言构造及其写法；
- `[代码变量]`：如 `attempt`，是代码作者给当前数据取的名字；
- `[标准技术术语]`：如幂等、唯一约束、异常传播；
- `[课程说法]`：本页为了方便说明临时采用的词，不要求当作标准术语记忆。

不能把模型临时创造的压缩词直接当成学生必须理解的术语。标准术语采用“现象 → 名称 → 代码例子”的顺序；如果普通中文已经足够，就不额外制造术语。

### 三种难度分离

每道题分别记录：

- **问题逻辑**：现实规则是否理解；
- **程序结构**：知道哪段代码负责、哪些路径稳定；
- **Python 表达**：能否识别或写出具体语法。

语法不会不能推断逻辑不会；逻辑理解也不代表 Python 表达已经恢复。帮助和学习记录必须保留这三个维度。

### 帮助菜单

阶段 A 每个任务至少提供五类帮助：

1. **我看不懂题目让我做什么**：把现实要求改写成当前动作，不泄露逻辑答案；
2. **我不知道要改哪段代码**：高亮 editable locus，说明哪些代码保持不动；
3. **我知道改哪里，但不知道 Python 怎么写**：给语法和最小写法；
4. **我知道写法，但不知道为什么这样改**：解释程序责任和边界；
5. **查看参考答案**：最后才给完整答案。

前四类帮助的使用不能被记录成“答对”；它们只说明学习者卡在哪一层。帮助等级应随同一 Python 构造的再次出现逐步淡出。

### 修改坡度

阶段 A 的代码定位和编辑范围按以下顺序逐渐撤掉：

```text
填一个明确槽位 → 修改一行高亮代码 → 修改一个高亮代码块
→ 在完整函数中定位 → 在文件中定位 → 在真实项目中定位
```

“以前学过 Python”只作为恢复速度的假设，不作为当前可调用语法的证据。课程应在真实任务中记录 `pass`、函数调用、参数、`if`、`for`、`range`、`except`、`raise`、列表、字典等构造是否已解释、是否仍需提示，而不是先假定整套语法已经可用。

## 其他页面身份

- **综合复习会话**：把已有学习记录重新连接成完整判断链；采用单步、三个综合任务与局部术语，但不自动新增记录，也不推进正式会话进度。
- **已完成复习页**：对应已有学习记录，只供可选复习；不得再称为当前练习或要求重新提交。
- **候选参考页**：尚未进入或等待重写的材料；不得出现在当前行动入口，必须声明未来归属会话。
- **参考卡**：服务于某个正式会话的压缩材料；必须标注归属和状态，不能直接用旧课编号解释位置。

## 当前会话序列

当前序列属于**阶段 A · 环 1“可执行 Python 与可靠节点”**，不是整门课程：

1. 状态与允许行动（已完成的短会话，作为体量校准样本）
2. 重试、防重复与批处理（进行中，包含三个不同问题；本次采用示范、局部补全和有界变化）
3. 模块、诊断与授权边界（未开始；会话开头先完成无答案间隔回忆，再依次学习三个问题：模块责任与调用链、traceback 与测试诊断、模型候选与已授权行动）

正式会话 2 结束后，在会话 3 前加入第一次无答案间隔回忆。

完整课程为四阶段、十七环；后续环级事实由 `reference/0002-course-progress.html` 唯一持有，最终能力由 `FINAL-CAPABILITY-CONTRACT.md` 唯一持有。
