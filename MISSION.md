# Mission：独立掌控可靠的 LLM、Agent 与人机决策系统

## Why

目前 Catalog 与 Feedback 两个项目主要由 Codex 编写，用户掌握方向，但尚不能独立承担完整的数学判断、代码实现、模型机制解释、故障诊断和综合交付。本课程要把这种依赖转化为真正的代码、决策与系统所有权，并为后续的人机合作和有限机制分析建立可计算入口。

## 唯一最终目标

课程完成并通过迁移任务与综合项目后，用户能够独立设计、实现、评估和维护一个可靠、受控、可恢复的 LLM 工作流或 RAG Agent；能够从现实需求定义对象、规则、状态、行动、损失、受影响者和失败边界，在有限表示、有限维、可计算模型中分析不确定性、单步决策、序贯策略、人机协作和多主体激励；能够把数学策略接入真实权限、人工容量与外部副作用，并用测试、求解、仿真、实验、指标、trace、故障注入、恢复和回滚证明系统行为。

同时能够阅读相关论文和实现，定位公式、张量形状、目标函数、实验结果与适用假设。课程达到“可靠的 LLM 应用、RAG、工作流、Agent 与人机决策系统工程”水平，并为一般或高级机制设计、一般状态空间理论、高级因果推断、预训练和架构研究建立明确入口。

## 最终能力域

1. 数学语言、概率、统计与证据；
2. 线性代数与张量计算；
3. 微积分、决策与约束优化；
4. 状态演化、随机过程与序贯决策；
5. 语言模型核心计算；
6. Transformer 机制；
7. 嵌入、检索与 RAG；
8. 优化训练、数值精度与计算排错；
9. API、工作流、Agent 与人机合作；
10. 博弈、信息与有限机制分析；
11. 实验、因果评估与系统可靠性。

完整、可核对的能力定义只由 [FINAL-CAPABILITY-CONTRACT.md](FINAL-CAPABILITY-CONTRACT.md) 持有；其他页面只能摘要和链接，不得另行扩大、缩小或重新定义最终承诺。

## Success looks like

- 能不靠文件名提示，画出 Catalog、Feedback 与综合项目的数据流、状态、决定权、失败路径和现实副作用。
- 能沿真实调用链定位模块和函数，独立完成中等跨模块 Python 改动、测试、诊断与回滚。
- 能用量词、条件、证明或反例表达规则；能计算有限联合分布、Bayes 后验、条件期望、置信区间、校准（calibration）和布里尔分数（Brier score），并说明证据边界。
- 能标注向量、矩阵和张量形状，建立含损失、容量、权限与公平约束的单步决策模型，使用枚举或求解器求解，解释起作用约束（binding constraint），并在条件变化后重新求解。
- 能手算有限马尔可夫链（Markov chain），用 Python 求解有限马尔可夫决策过程（MDP），并用仿真比较固定策略和一种多臂老虎机（bandit）策略；能区分工作流状态、环境状态与 Agent 信念。
- 能实现最小语言模型计算、attention 与 Transformer 前向过程，并诊断 shape、mask、数值精度和训练曲线问题。
- 能建立可追踪的检索、RAG、工作流和 Agent，把校准、风险—覆盖率、人工容量和权限接入自动、询问、升级、拒绝或延迟策略。
- 能在有限类型和报告集合上计算主体效用，检查占优策略激励相容（DSIC）或贝叶斯激励相容（BIC）、个体理性（IR）、效率与预算，找出操纵路径或给出有限范围内的穷举验证。
- 能定义估计目标（estimand），区分描述性、统计与因果结论，在有限因果有向无环图（DAG）中识别背门路径、碰撞点和候选调整集，并标出图结构假设与无法识别的部分。
- 能设计任务级、组件级和端到端评估，测量质量、成本、P95 延迟、吞吐与可靠性，并用版本、trace、回归、SLO、停止和回滚控制变化。
- 能在同一个综合项目中贯通现实目标、决策策略、序贯反馈、主体激励、因果评估与可靠工程，而不是提交彼此无关的理论作业。

## Constraints

- 中文讲解；英文首次出现时说明中文含义，技术标识、路径、命令和公式保持原文。
- 课程按四阶段、十七环推进；当前“阶段 A · 环 1”由三个正式会话组成，只是整门课程的起点。
- 主线优先使用有限概率空间、有限状态/行动/时间、有限类型博弈、有限容量与任务集合、有限维连续优化和可枚举、可求解、可仿真或可数值核验的模型；有限不等于只做离散数学。
- 一个知识节点服务于某个问题；一个问题建立一个中心能力，并完整经过定向、底层逻辑、具体细节、预测、核心实现、条件迁移和小结七步；一个 30–45 分钟正式会话通常安排三个不同问题（共九项任务），并在末尾统一互动。不能用同一问题的重复解释或同类任务填充会话时长；每 2–3 个会话安排一次无答案间隔回忆。
- 页面一次只显示一个当前步骤，但必须分别说明抽象底层逻辑、数学或系统关系、具体编程细节和适用边界。
- 项目与理论采用双向螺旋；后续课程必须遵守“环 5 形成信念 → 环 7 单步选择 → 环 8 跨时间选择 → 环 9 多主体规则 → 环 14 接入组织与权限 → 环 15 验证真实效果”的责任边界。
- 自动检查只提供即时反馈；只有解释、证明或反例、修改、诊断、手算、穷举、求解器、仿真、公式核验、变式实现或跨情境迁移才能推进能力关口。
- 会计算有限模型不等于掌握一般理论，仿真结果不等于数学证明，数学最优行动不等于组织已经授权执行。
- 已验证的 11 条学习记录继续有效；通过综合复习重新连接，不要求按旧页面逐题重做。
- 第一轮优先本地模拟、小数据和有限模型，不要求付费 API、GPU 或真实生产写入。

## Out of scope

- 本课程不声称培养完整的实分析、测度概率、一般或连续状态空间随机过程、一般 POMDP 求解、完整强化学习或高维统计推断能力。
- 不声称培养完整 do-calculus、复杂工具变量、高维因果机器学习或一般动态处理制度能力。
- 不声称掌握一般 Bayesian revelation principle、Myerson 最优拍卖、多维类型、动态机制设计、复杂算法机制设计或高级市场设计；主线只承诺有限机制分析与实现。
- 系统性 LoRA 实验、FP16/BF16 混合精度、量化误差深入分析、精细显存分解与完整训练调参，以及数学物理、连续场、波、一般变分问题、完整大模型预训练、架构研究、GPU 内核、分布式训练和高性能计算属于研究扩展。
- 不以看完页面、自动检查通过、生成代码、背出术语或复述公式代替可迁移能力。
- 不为了覆盖更多名词牺牲真实代码、数学核验、失败证据和综合项目；任何主线术语都必须有明确责任、计算产物和通过关口。
