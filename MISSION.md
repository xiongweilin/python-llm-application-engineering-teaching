# Mission：独立掌控可靠的 LLM 应用、RAG、工作流与 Agent 工程

## Why

目前 Catalog 与 Feedback 两个项目主要由 Codex 编写，用户掌握方向，但尚不能独立承担完整的数学判断、代码实现、模型机制解释、故障诊断和综合交付。本课程要把这种依赖转化为真正的代码与系统所有权。

## 唯一最终目标

课程完成并通过迁移任务与综合项目后，用户能够独立设计、实现、评估和维护一个受控的 LLM 工作流或 RAG Agent；能够从现实需求定义对象、规则、状态、受影响者和失败边界，理解关键概率与张量计算，选择单次调用、固定工作流或 Agent，控制权限与外部副作用，并用测试、指标、trace、故障注入、恢复和回滚证明系统行为。

同时能够阅读相关论文和实现，定位公式、张量形状、目标函数、实验结果与适用假设。课程达到“可靠的 LLM 应用、RAG、工作流和 Agent 工程”水平，为进一步学习预训练和架构研究建立入口。

## 最终能力域

1. 数学表示与证据判断；
2. 微积分、线性代数与张量计算；
3. 动力系统与物理结构；
4. 语言模型核心计算；
5. Transformer 机制；
6. 嵌入、检索与 RAG；
7. 优化、训练与轻量微调；
8. 数值精度、显存与计算排错；
9. API、工作流与 Agent 工程；
10. 多主体、规则和长期后果；
11. LLM 系统评估与可靠性。

完整、可核对的能力定义由 [FINAL-CAPABILITY-CONTRACT.md](FINAL-CAPABILITY-CONTRACT.md) 唯一持有；其他页面只能摘要和链接，不得另行缩小最终承诺。

## Success looks like

- 能不靠文件名提示，画出 Catalog、Feedback 与综合项目的数据流、状态、决定权、失败路径和现实副作用。
- 能沿真实调用链定位模块和函数，独立完成中等跨模块 Python 改动、测试、诊断与回滚。
- 看到概率、向量、矩阵或张量公式时，能说明对象含义、单位、条件与形状，并用 NumPy 或 PyTorch 核验。
- 能实现最小语言模型计算、注意力与 Transformer 前向过程，并诊断 shape、mask、数值精度和训练曲线问题。
- 能建立可追踪的检索、RAG、工作流和 Agent，区分结构合法、内容正确、执行授权与现实结果。
- 能设计任务级、组件级和端到端评估，测量质量、成本、P95 延迟、吞吐与可靠性。
- 能把并发、重试、幂等、恢复、权限、机制和受影响者纳入同一个长期系统判断。
- 能在综合项目中主导需求、架构、实现、评估、故障注入、人工复核、停止、恢复、回滚和答辩。

## Constraints

- 中文讲解；英文首次出现时说明中文含义，技术标识、路径、命令和公式保持原文。
- 课程按四阶段、十七环推进；当前“阶段 A · 环 1”由三个正式会话组成，只是整门课程的起点。
- 一个知识节点服务于某个问题；一个问题建立一个中心能力，并完整经过定向、底层逻辑、具体细节、预测、核心实现、条件迁移和小结七步；一个 30–45 分钟正式会话通常安排三个不同问题（共九项任务），并在末尾统一互动。不能用同一问题的重复解释或同类任务填充会话时长；每 2–3 个会话安排一次无答案间隔回忆。
- 页面一次只显示一个当前步骤，但必须分别说明抽象底层逻辑、数学或系统关系、具体编程细节和适用边界。
- 数学、物理与模型机制采用项目牵引和独立关口并行：可以在项目需要时提前出现，但不能因为项目暂时未触发而被删除。
- 自动检查只提供即时反馈；只有解释、修改、诊断、公式核验、变式实现或跨情境迁移才能推进能力关口。
- 已验证的 11 条学习记录继续有效；通过综合复习重新连接，不要求按旧页面逐题重做。
- 第一轮优先本地模拟和小数据，不要求付费 API、GPU 或真实生产写入。

## Out of scope

- 本课程不声称培养完整的大模型预训练、架构研究、GPU 内核、分布式训练或高性能计算能力。
- 不以看完页面、自动检查通过、生成代码、背出术语或复述公式代替可迁移能力。
- 不为了覆盖更多名词牺牲真实代码、数学核验、失败证据和综合项目。
- 不把物理类比当证明；物理结构必须通过方程、计算或可验证模型学习。
