# 教学状态与约束

## Current state

- 当前日期：2026-08-18。
- 已验证学习记录：11；保持原表述，不把接触、同例模仿或自动检查升级为更高能力。
- 第 1 题已通过：用户能区分递归与链式调用，并说明不可变字符串与可变列表对副作用的不同影响。
- 第 2 题已通过：用户能区分累计全部错误与首错即停对控制流、接口约定和测试的影响，并理解规则顺序决定首个错误。
- 正式学习会话 1“状态与允许行动”已通过：用户能预测、实现并迁移受限制的状态转换，且能区分稳定控制流、转换数据和接口约定。
- 当前任务：完成正式学习会话 2“受控重试：完整工作单元”；本会话不再把 retry、防重复、批处理三个模式并列塞在同一页面，而是让同一个 retry 模式经历完整获得、受控变化、迁移与组块化。
- 10 条已有学习记录已整合为综合复习会话 R1；它是日常复习入口，不改变正式会话进度，也不自动形成新记录。
- 阶段 A · 环 1 的序列为：会话 1“状态与允许行动”（已完成，作为体量校准样本）；会话 2“受控重试：完整工作单元”（进行中）；会话 3“模块、诊断与授权边界”（未开始，开头安排无答案间隔回忆，再学习模块责任与调用链、traceback 与测试诊断、模型候选与已授权行动）。防重复与批处理保留为历史记录，后续分别安排完整工作单元。
- 完整课程为四阶段、十七环；四阶段范围相加为约 90–125 个正式会话的规划量级，当前环 1 的三个会话不是整门课程。
- 活动课程只有一个首页、一个进度页和一个 Windows 开始菜单入口。

## Teaching preferences

- 中文优先；英文首次出现时给出中文含义，技术标识保持原样。
- 新概念依次提供中文定义、关系或流程、小例子和对应 Python 写法；不能只给术语或结论。
- 阶段 A 的基本教学单位是“完整、可观察、可验证的工程工作单元”，不固定为孤立函数；也可以是完整诊断过程、调用链理解或局部修复与回归。阶段 A 关口 A1 关注脚手架下的完整重建、变式、诊断和迁移。
- 一个正式会话原则上围绕一个主要工作单元，目标体量约 30–45 分钟。会话内三个任务是三类能力证据：模式获得、受控变化、迁移与组块化，不是三个互不相关的问题。
- 页面只突出一个当前动作，其余材料逐步展开；点击步骤后滚动到活动内容顶部，不自动滚回页面上半部。
- 正式会话优先采用“完整范例 → 完整模仿 → 完整重建 → 受控变化 → 新情境迁移 → 稳定骨架与可变参数抽取”的循环。
- 每 2–3 个正式会话进行一次无答案间隔回忆，并混入更早阶段内容；回忆可以只要求控制流骨架、责任地图或结构角色，不要求语法完全正确。
- 学习者画像是“结构记忆仍在、符号调用暂时失效”的再激活型学习者：语法卡降低检索摩擦，但 R/T/P/M/D/X 证据仍必须分开记录。模仿不等于掌握，查询语法不等于结构缺失。
- 完整范例中的 Python 代码逐行旁注：每行说明对象、输入/输出、控制流、副作用、放置原因和相邻代码关系，减少来回翻找。
- 既有程序默认只读展示；第一次出现 `pass` 时解释它是合法 Python 语句，表示类体故意不增加行为，不是 TODO 或练习占位符。
- 阶段 A 统一采用“现实含义 → 程序责任 → 代码位置 → Python 写法 → 学生操作 → 运行验证”；不得从现实逻辑直接跳到完整 Python 修改。
- 第一次术语必须标来源：[普通中文]、[程序概念]、[Python 语法]、[代码变量]、[标准技术术语] 或 [课程说法]；普通中文已经足够时不制造压缩词。
- 数量/状态变量先说明“含义、类型/单位、取值、变化时机、改变者、影响步骤”；程序结构说明“做什么、何时执行、控制哪段代码、执行后去哪里”；函数/接口说明“输入、输出、副作用、失败和调用条件”。
- 过程题先展开“执行 → 成功/失败 → 是否还有下一次 → 若有则计算候选值 → 产生副作用 → 下一次执行”；必须分开“是否继续/是否发生副作用”和“如果发生，值是多少”。
- 预测必须先于运行验证；至少覆盖正常、关键失败、边界和一个未见条件；先预测、再运行、再比较。
- 禁止答案回显题：题干或示范已经给出的序列、答案或结构不能再用相同选项回填；错误项必须对应可解释的错误模型。
- 每个工作单元分别记录 `R`（Recognize）、`T`（Trace）、`P`（Produce）、`M`（Modify）、`D`（Diagnose）、`X`（Transfer），不以“题目通过”作为单一能力结论。
- 完整重建采用渐进撤除：完整参考代码 → 关键代码行 → 代码块标题 → 自然语言步骤 → 函数签名与测试 → 只有行为契约；失败时回退一级，不把语法遗忘直接判为结构失败。
- 迁移标注 Level 1–5：情境变化、参数变化、边界变化、结构维度变化、真实项目模式定位；只改函数名不记录为高阶迁移。
- 程序模式必须同时记录稳定骨架和可变参数，不能把参考代码当作必须逐字复刻的模板。
- 帮助菜单固定包含“题目让我做什么、观察/改哪段代码、Python 怎么写、为什么这样改、参考答案”五类；帮助使用与结构能力分开记录。页面使用 editable locus 明确练习位置，避免学习者从整段代码的视觉空白猜题。
- 编辑坡度为“填槽位 → 改一行 → 改一个代码块 → 函数内定位 → 文件内定位 → 项目内定位”；当前会话的完整重建允许查询语法，但不提供整段参考答案。
- 阶段 A 后半段逐步从 syntax recovery 转向代码地图、控制流/数据流、测试/traceback 诊断、局部修改和未见条件验证；完整开放式建模仍放到后续阶段，但每 2–3 个正式会话必须安排一个不计关口的开放问题窗口，让学习者先暴露未知量和证据缺口。
- 课程同时运行两个循环：技能循环“范例 → 重建 → 受控修改 → 迁移 → 说明稳定结构”，判断循环“真实问题 → 当前判断 → 专家视角 → 缺口 → 有目的补基础 → 重新判断”。
- 阶段 A 的开放问题窗口约 5 分钟，只要求回答“我现在会怎么判断、最不确定什么、需要补什么”；不要求初学者当场设计完整系统，也不把窗口答案自动记录为能力通过。
- 每个开放问题窗口必须保存判断版本（如 `J0`），在相关后续会话中重新取出并形成 `J1/J2/...`；记录改变了什么、哪条证据触发改变、仍保留什么不确定性。开放窗口不能只做一次性漂亮讨论。
- 增加 Minimum Viable Judgment（最低可判断能力）作为教学模式切换阈值，不是毕业关口。工作域在陌生受控任务、无答案间隔回忆和未见失败上持续表现后，默认从“行为契约 → 学习者先判断 → 实现/诊断”开始，失败时按证据恢复脚手架。
- MVJ 必须按领域分别定义：Python/工程关注代码地图与控制流证据；概率关注随机变量、模型假设与证据边界；优化关注变量/目标/约束/敏感性；序贯关注状态/转移/策略/未来影响；RAG/Agent 关注检索—生成—工具—权限分解与评估；因果/可靠性关注 estimand、识别假设和实验证据。一个领域跨阈值不代表其他领域也跨阈值。
- 阈值前的调度约为“80% 能力形成 + 20% 开放问题窥视”；刚跨阈值约为“60% 能力形成 + 40% 判断”；稳定迁移后约为“30% 针对性补基础 + 70% 真实问题”。这些是调度启发式，不是评分规则。
- `Frame / Discriminate / Judge / Challenge / Revise` 作为教师记录的第二层判断动作，观察问题表述、候选区分、证据选择、反例挑战和新证据下的修正；不要求学习者背诵这套英文缩写。
- AI intervention contract：学习者能力上升时答案供给下降、问题空间供给上升；早期 AI 是解释器/示例生成器/即时诊断器，中期是教练/对抗测试器，后期是高水平讨论对象/候选映射生成器。AI 必须要求预测先于运行，按领域 MVJ 和证据选择介入层级，并在问题超过当前基础时明确标记而不是用高级内容淹没缺口。
- 相似工作模式的迁移帮助逐次撤除：先由教师标稳定骨架和变化点，再由学习者标，再比较相似实现，最后判断真实故障是否仍适合该抽象。
- 阶段 A 出题审查必须检查现实事件、量的语义、时序、唯一未知量、编辑范围、预测—运行—比较、非回显、错误定位和近邻迁移是否清楚。
- 自动检查只负责快速反馈；学习记录要求重建、解释、诊断、修改或迁移证据。
- 用户提交完整会话后，如果证据达到通过标准，应在同一轮只记录本次能力证据并给出下一会话；首页、路线、术语表和看板等引用页面等用户明确说“今天的课程就学到这里”后再统一同步。

## Course design decisions

- 两个项目（Commerce Orchestrator 与 DeepSeek Harness）采用纵向切片，不先逐文件讲完所有模块。
- 每个切片都走架构、模块、节点、验证、迁移五层，并在另一个项目中对照。
- 数学、概率、张量和模型原理采用项目牵引与独立关口并行；有限模型计算不冒充一般理论，仿真不冒充证明。
- 环级责任固定为“环 5 形成信念 → 环 7 单步选择 → 环 8 跨时间选择 → 环 9 多主体规则 → 环 14 接入权限与人工容量 → 环 15 验证真实效果”。
- 最终能力只由 `FINAL-CAPABILITY-CONTRACT.md` 持有；完整环级路线只由 `reference/0002-course-progress.html` 持有，其他页面只摘要和链接。
- 全部 HTML 页面必须显示统一最终目标和当前页面身份；学习位置只用“学习环 → 正式会话”表达。
- 活动文档保持最小且充分：README 只负责入口，`FINAL-CAPABILITY-CONTRACT.md` 负责毕业能力，`reference/0002-course-progress.html` 负责环级路线和关口覆盖，`SESSION-PAGE-CONTRACT.md` 负责会话页面规范，NOTES 负责当前状态与生成约束，RESOURCES 负责来源，ADR 负责决策原因。
- 全课程定位是有限、可计算、可调用的 minimum operational models，不宣称压缩一个完整数学、计算机科学或机制设计学位；一般理论、连续/无限状态空间和复杂识别继续作为研究扩展。
- 每个正式会话页面下方必须有本会话中文术语表；总术语表只承担跨会话索引。
- 正式会话 3“模块、诊断与授权边界”的问题三“模型候选与已授权行动”仍采用示范、诊断和有界变化，不代表提前进入完整 LLM 阶段。
- 教学架构变更记录在 `docs/decisions/0007-phase-a-whole-work-unit-cycle.md`；旧 ADR 保留作历史，不再作为当前阶段 A 的粒度规范。
- 开放问题窗口、Minimum Viable Judgment 和能力证据驱动的教学模式切换记录在 `docs/decisions/0008-open-judgment-and-threshold-switch.md`；不改变当前正式会话 2 的页面内容。
- 判断版本轨迹、分领域 MVJ、AI intervention contract 和最终项目适用性判断记录在 `docs/decisions/0009-domain-thresholds-judgment-history-and-ai-intervention.md`；不改变当前正式会话 2 的页面内容。

## Source hierarchy

1. 当前项目源码、测试、迁移、配置和运行证据。
2. 项目 README、ADR、数据契约与两份 D 盘教学指南。
3. Python、PostgreSQL、PyTorch 和供应商当前官方文档。
4. 计算机、教育、数学、组织领域模型与有限宇宙方法文档。
5. 教师解释只形成待检验结构，不能替代以上证据。

## Calibration still needed

- 完整重建在不同帮助层级下能否稳定形成，而不是只在同例模仿时成功。
- 是否能用 pytest 和 traceback 自行定位失败，并把局部症状压缩成责任块。
- Level 1–3 的近邻迁移是否足以区分“记住模板”和“获得模式”。
- NumPy、概率、矩阵和导数的真实起点。
- API、RAG、向量检索和 Agent 的既有实践深度。
- 每周实际投入时间、API 预算和可用 GPU 条件。
