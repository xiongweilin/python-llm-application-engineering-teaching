# 教学状态与约束

## Current state

- 当前日期：2026-07-31。
- 已验证学习记录：11；保持原表述，不把接触或自动检查升级为更高能力。
- 第 1 题已通过：用户能区分递归与链式调用，并说明不可变字符串与可变列表对副作用的不同影响。
- 第 2 题已通过：用户能区分累计全部错误与首错即停对控制流、接口约定和测试的影响，并理解规则顺序决定首个错误。
- 正式学习会话 1“状态与允许行动”已通过：用户能预测、实现并迁移受限制的状态转换，且能区分稳定控制流、转换数据和接口约定。
- 当前任务：完成正式学习会话 2“重试、防重复与批处理”的三个不同问题，并统一提交一次。
- 10 条已有学习记录已整合为综合复习会话 R1；它是日常复习入口，不改变正式会话进度，也不自动形成新记录。
- 阶段 A · 环 1 由三个正式会话组成：会话 1“状态与允许行动”（已完成的短会话，作为体量校准样本）；会话 2“重试、防重复与批处理”同时负责“失败、重试与停止”“操作身份与防重复”“批处理与错误边界”三个问题；会话 3“模块、诊断与授权边界”将在开头安排无答案间隔回忆，并负责“模块责任与调用链”“traceback 与测试诊断”“模型候选与已授权行动”三个问题。
- 完整课程为四阶段、十七环，约 90–120 个正式会话的规划量级；当前环 1 的三个会话不是整门课程。
- 活动课程只有一个首页、一个进度页和一个 Windows 开始菜单入口。

## Teaching preferences

- 中文优先；英文首次出现时给出中文含义，技术标识保持原样。
- 新概念依次提供中文定义、关系或流程、小例子和对应 Python 写法；不能只给术语或结论。
- 一个问题只训练一个中心能力，必须有真实代码、测试或可运行计算；正式会话是多个问题的学习时间容器，不等于单个问题。
- 用户容易被并列信息分散注意；页面应只突出一个当前动作，其余材料逐步展开。
- 正式页面以 A“专注隧道”为主：一次只显示一个步骤；详细解释不能省略，但抽象底层逻辑与具体编程细节应拆成相邻步骤。
- 每 2–3 个正式会话进行一次无答案间隔回忆，并混入更早阶段内容。
- 职责辨析把多个模块混入同一道题，不按文件标题泄露答案。
- 提示按“解释代码 → 先预测 → 独立定位 → 独立设计”递减。
- 自动检查只负责快速反馈；学习记录要求解释、修改、诊断或迁移证据。
- 每个问题由网页即时反馈；通常三个不同问题（共九项任务）组成一次 30–45 分钟正式会话，结束后再与老师互动一次。
- 用户提交完整会话后，如果证据达到通过标准，应在同一轮只记录本次能力证据并给出下一会话；只有存在必须补答的关键缺口时才暂停。学习日内不逐次更新首页、路线、进度页、术语表、看板等引用页面；等用户明确说“今天的课程就学到这里”后，再一次性同步并统一验证。
- 每个正式会话按目标体量 30–45 分钟设计，同时在提交中记录实际投入分钟数；后续体量以用户实际用时和能力证据校准，而不是只按页面数量估算。
- 粒度层级固定为“知识节点 → 问题 → 正式会话”。多个节点即使共同服务于同一个问题，仍只算一个问题。30–45 分钟体量应来自一个会话安排约三个不同问题，每题通常 10–15 分钟；不能靠重复解释、增加同题任务或把单个问题拉长。若问题数量不足，应降低目标时长或调整总路线与会话数量。
- 每个问题不得因同一会话包含多个问题而压缩深度；统一采用会话 1 已验证的七步闭环：定向、底层逻辑、Python/计算细节、预测、核心实现、条件变化后的迁移、问题小结。三个问题的正式会话因此通常包含 21 个问题步骤（三个问题共九项任务），另加会话地图和统一提交。
- 已有学习记录需要整体复习时，优先按一条系统链组织为单一综合复习会话；旧单项页只保留为证据细节入口。

## Course design decisions

- 两个项目采用纵向切片，不先逐文件讲完所有模块。
- 每个切片都走架构、模块、节点、验证、迁移五层，并在另一个项目中对照。
- 数学、概率、张量和模型原理采用项目牵引与独立关口并行；可以在聚类、评估、RAG 或模型故障前提前进入，但不能因项目未触发而删除。
- 最终能力只由 `FINAL-CAPABILITY-CONTRACT.md` 持有；完整环级路线只由 `reference/0002-course-progress.html` 持有，其他页面只摘要和链接。
- 全部 HTML 页面必须显示统一最终目标和当前页面身份，并能进入完整路线与最终能力契约。
- Transformer 与轻量微调是解释应用机制的深度支线，不阻塞 Python 与项目代码所有权。
- 正式会话 3“模块、诊断与授权边界”的问题三“模型候选与已授权行动”是环 1 的早期迁移关口，不代表提前进入完整 LLM 阶段；文件 0009 只是等待重写的候选材料。
- 每个正式会话页面下方必须有本会话中文术语表；总术语表只承担跨会话索引，不能替代局部解释。
- 学习位置只用“学习环 → 正式会话”表达；文件编号、旧课编号和参考卡都不能成为主路线坐标。
- “一个中心能力”对应一个问题，不对应整个正式会话；正式会话把多个问题编排在同一页面，逐题即时验证，末尾统一提交。
- 路线页必须同时说明整个课程的学习目的、完成后掌握的底层逻辑整体和编程细节整体，不能只列阶段名称。
- 三种界面原型已完成选择并退出活动入口；正式会话吸收 A 的单步骨架、C 的解释深度和 B 的极简位置提示。

## Source hierarchy

1. 当前项目源码、测试、迁移、配置和运行证据。
2. 项目 README、ADR、数据契约与两份 D 盘教学指南。
3. Python、PostgreSQL、PyTorch 和供应商当前官方文档。
4. 计算机、教育、数学、组织领域模型与有限宇宙方法文档。
5. 教师解释只形成待检验结构，不能替代以上证据。

## Calibration still needed

- 六道 Python 函数在需求变化后能否独立修改。
- 是否能用 pytest 和 traceback 自行定位失败。
- NumPy、概率、矩阵和导数的真实起点。
- API、RAG、向量检索和 Agent 的既有实践深度。
- 每周实际投入时间、API 预算和可用 GPU 条件。
