# 教学状态与约束

## Current state

- 当前日期：2026-08-17。
- 已验证学习记录：11；保持原表述，不把接触或自动检查升级为更高能力。
- 第 1 题已通过：用户能区分递归与链式调用，并说明不可变字符串与可变列表对副作用的不同影响。
- 第 2 题已通过：用户能区分累计全部错误与首错即停对控制流、接口约定和测试的影响，并理解规则顺序决定首个错误。
- 正式学习会话 1“状态与允许行动”已通过：用户能预测、实现并迁移受限制的状态转换，且能区分稳定控制流、转换数据和接口约定。
- 当前任务：完成正式学习会话 2“重试、防重复与批处理”的三个不同问题，并统一提交一次；页面采用面向再激活型学习者的建议路径，默认先完成约 12 个高价值动作，完整 23 步仍可展开；不要求从空白设计实现。
- 10 条已有学习记录已整合为综合复习会话 R1；它是日常复习入口，不改变正式会话进度，也不自动形成新记录。
- 阶段 A · 环 1 由三个正式会话组成：会话 1“状态与允许行动”（已完成的短会话，作为体量校准样本）；会话 2“重试、防重复与批处理”同时负责“失败、重试与停止”“操作身份与防重复”“批处理与错误边界”三个问题；会话 3“模块、诊断与授权边界”将在开头安排无答案间隔回忆，并负责“模块责任与调用链”“traceback 与测试诊断”“模型候选与已授权行动”三个问题。
- 完整课程为四阶段、十七环；四阶段范围相加为约 90–125 个正式会话的规划量级，当前环 1 的三个会话不是整门课程。
- 活动课程只有一个首页、一个进度页和一个 Windows 开始菜单入口。

## Teaching preferences

- 中文优先；英文首次出现时给出中文含义，技术标识保持原样。
- 新概念依次提供中文定义、关系或流程、小例子和对应 Python 写法；不能只给术语或结论。
- 一个问题只训练一个中心能力，必须有真实代码、测试或可运行计算；正式会话是多个问题的学习时间容器，不等于单个问题。
- 用户容易被并列信息分散注意；页面应只突出一个当前动作，其余材料逐步展开。
- 正式页面以 A“专注隧道”为主：一次只显示一个步骤；详细解释不能省略，但抽象底层逻辑与具体编程细节应拆成相邻步骤。
- 每 2–3 个正式会话进行一次无答案间隔回忆，并混入更早阶段内容。
- 职责辨析把多个模块混入同一道题，不按文件标题泄露答案。
- 阶段 A 的提示按“教师示范 → 共同追踪 → 局部补全 → 有界变化”递减；独立定位和独立设计主要放在后续阶段或已明确标注的关口。
- 阶段 A 仍采用示范优先、局部补全和有界变化；再激活路径只是改变帮助和导航的呈现方式。
- 阶段 A 的可见 Python 代码逐行旁注：每行说明对象、输入/输出、控制流、副作用或放置原因；旁注解释为什么，不只重复语法名称，减少学习者在页面之间来回翻找。
- 学习者画像是“结构记忆仍在、符号调用暂时失效”的再激活型学习者：语法卡可以降低检索摩擦，但结构追踪、预测、诊断和有界迁移仍必须保留。帮助按语法点记录并逐次淡出，语义答案放在折叠提示中，不能直接写进默认任务旁注。
- 局部修正任务必须显式区分既有程序和练习位置：代码主体只读展示，单独提供 editable locus；第一次出现 `pass` 时解释它是合法空操作语句，不是 TODO 或待填写占位符，不能让学习者靠视觉猜题。
- 有界变化不能只说“先列出对应关系再改表达式”：必须先给出对应关系表和边界条件，说明变量值如何映射到输出/副作用值，再解释表达式位于哪个括号内，最后才让学习者编辑唯一输入。
- 阶段 A 统一采用“现实含义 → 程序责任 → 代码位置 → Python 写法 → 学生操作 → 运行验证”；不得从现实逻辑直接跳到完整 Python 修改。
- 阶段 A 的第一次术语必须标来源：[普通中文]、[程序概念]、[Python 语法]、[代码变量]、[标准技术术语] 或 [课程说法]；先解释现象，再给名称和代码例子，禁止模型自创压缩词冒充标准术语。
- 每道题分开记录问题逻辑、程序结构和 Python 表达三种难度；帮助菜单固定包含“题目让我做什么、改哪段代码、Python 怎么写、为什么这样改、参考答案”五类。
- 早期编辑坡度固定为“填槽位 → 改一行 → 改代码块 → 函数内定位 → 文件内定位 → 项目内定位”；“以前学过 Python”不再作为当前语法可调用的证据。
- 阶段 A 出题注意事项：所有量先写“含义、单位/类型、变化时机、改变者、影响步骤”；连续过程先展开“执行 → 成功/失败 → 是否还有下一次 → 候选值 → 副作用 → 下一次执行”。
- 阶段 A 必须分开“策略计算出的候选值”和“具体运行中实际发生的副作用”，也必须分开“是否等待”和“等待多久”；每个可操作的小题只保留一个主要未知量，多步有界变化必须顺序解锁。
- 禁止答案回显题：题干或示范已给出的序列不能再用相同选项要求回填；选择题错误项必须对应可诊断的错误模型。发布前检查现实事件、量的单位、事件顺序、输入、输出、唯一未知量、编辑范围和错误定位是否唯一。
- 阶段 A 后半段逐步从 syntax recovery 转向诊断证据；代码所有权关口看代码地图、控制流/数据流、测试/traceback、局部修改和未见条件验证，不把 API 记忆当作毕业条件。
- 阶段 A 的正式会话同时提供“建议路径”和“完整讲解”：建议路径只减少导航和重复解释，不删除七步闭环、九项任务或统一提交证据。
- 自动检查只负责快速反馈；学习记录要求解释、修改、诊断或迁移证据。
- 每个问题由网页即时反馈；通常三个不同问题（共九项任务）组成一次 30–45 分钟正式会话，结束后再与老师互动一次。早期会话的九项任务必须由示例、近完整脚手架和明确改动位置支撑，不能用空白实现制造“无中生有”的难度。
- 用户提交完整会话后，如果证据达到通过标准，应在同一轮只记录本次能力证据并给出下一会话；只有存在必须补答的关键缺口时才暂停。学习日内不逐次更新首页、路线、进度页、术语表、看板等引用页面；等用户明确说“今天的课程就学到这里”后，再一次性同步并统一验证。
- 每个正式会话按目标体量 30–45 分钟设计，同时在提交中记录实际投入分钟数；后续体量以用户实际用时和能力证据校准，而不是只按页面数量估算。
- 粒度层级固定为“知识节点 → 问题 → 正式会话”。多个节点即使共同服务于同一个问题，仍只算一个问题。30–45 分钟体量应来自一个会话安排约三个不同问题，每题通常 10–15 分钟；不能靠重复解释、增加同题任务或把单个问题拉长。若问题数量不足，应降低目标时长或调整总路线与会话数量。
- 每个问题不得因同一会话包含多个问题而压缩深度；统一采用七步闭环：定向、教师示范与底层逻辑、Python/计算细节、预测、引导式局部补全、一个有界变化、问题小结。三个问题的正式会话因此通常包含 21 个问题步骤（三个问题共九项任务），另加会话地图和统一提交。开放式建模、方案比较和独立设计主要放在后几个阶段。
- 已有学习记录需要整体复习时，优先按一条系统链组织为单一综合复习会话；旧单项页只保留为证据细节入口。

## Course design decisions

- 两个项目（Commerce Orchestrator 与 DeepSeek Harness）采用纵向切片，不先逐文件讲完所有模块。
- 每个切片都走架构、模块、节点、验证、迁移五层，并在另一个项目中对照。
- 数学、概率、张量和模型原理采用项目牵引与独立关口并行；可以在聚类、评估、RAG 或模型故障前提前进入，但不能因项目未触发而删除。
- 后续数学主线采用有限表示、有限维、可计算优先；有限不等于只做离散数学，有限模型计算不代表一般理论，仿真不代表证明。
- 环级责任固定为“环 5 形成信念 → 环 7 单步选择 → 环 8 跨时间选择 → 环 9 多主体规则 → 环 14 接入权限与人工容量 → 环 15 验证真实效果”；校准、Pareto、排队和评估不得在这些环之间重复承担同一教学责任。
- 主线毕业能力是有限机制分析与实现；数学物理、一般状态空间理论、高级因果和高级机制设计属于研究扩展，不再作为毕业关口。
- 最终能力只由 `FINAL-CAPABILITY-CONTRACT.md` 持有；完整环级路线只由 `reference/0002-course-progress.html` 持有，其他页面只摘要和链接。
- 全部 HTML 页面必须显示统一最终目标和当前页面身份，并能进入完整路线与唯一最终能力事实源；不再维护与 Markdown 事实源同质的 HTML 摘要页。
- 活动文档保持最小且充分：README 只负责入口，`FINAL-CAPABILITY-CONTRACT.md` 负责毕业能力，`reference/0002-course-progress.html` 负责环级路线和关口覆盖，`SESSION-PAGE-CONTRACT.md` 负责会话页面规范，NOTES 负责当前状态与生成约束，RESOURCES 负责来源，ADR 负责决策原因。
- Transformer 与训练数值核心用于解释应用机制；系统 LoRA、混合精度、深入量化、精细显存和完整调参是按需扩展，不阻塞 Python 与项目代码所有权，也不属于主线毕业关口。
- 正式会话 3“模块、诊断与授权边界”的问题三“模型候选与已授权行动”是环 1 的早期边界识别关口，仍应采用示范和有界变化，不代表提前进入完整 LLM 阶段；文件 0009 只是等待重写的候选材料。
- 每个正式会话页面下方必须有本会话中文术语表；总术语表只承担跨会话索引，不能替代局部解释。
- 学习位置只用“学习环 → 正式会话”表达；文件编号、旧课编号和参考卡都不能成为主路线坐标。
- “一个中心能力”对应一个问题，不对应整个正式会话；正式会话把多个问题编排在同一页面，逐题即时验证，末尾统一提交。
- 路线页必须同时说明整个课程的学习目的、完成后掌握的底层逻辑整体和编程细节整体，不能只列阶段名称。
- 三种界面原型已完成选择并退出活动入口；正式会话吸收 A 的单步骨架、C 的解释深度和 B 的极简位置提示。
- 教学坡度决策记录在 `docs/decisions/0002-guided-teaching-before-independent-transfer.md`：阶段 A 先示范和追踪，再局部补全和有界迁移；后续阶段再逐步提高独立建模和开放迁移比例。
- 再激活型学习者与分层帮助决策记录在 `docs/decisions/0003-reactivation-learner-and-fading-support.md`：语法检索失败不等于结构能力缺失，建议路径与完整讲解共享同一证据，语义答案不放在默认旁注中。

## Source hierarchy

1. 当前项目源码、测试、迁移、配置和运行证据。
2. 项目 README、ADR、数据契约与两份 D 盘教学指南。
3. Python、PostgreSQL、PyTorch 和供应商当前官方文档。
4. 计算机、教育、数学、组织领域模型与有限宇宙方法文档。
5. 教师解释只形成待检验结构，不能替代以上证据。

## Calibration still needed

- 六道 Python 函数能否先在示范和近完整脚手架下完成局部修改，再逐步验证是否具备独立修改能力。
- 是否能用 pytest 和 traceback 自行定位失败。
- NumPy、概率、矩阵和导数的真实起点。
- API、RAG、向量检索和 Agent 的既有实践深度。
- 每周实际投入时间、API 预算和可用 GPU 条件。
