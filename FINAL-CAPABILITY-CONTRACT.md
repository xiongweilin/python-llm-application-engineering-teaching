# 最终能力契约

状态：完整课程的唯一最终能力事实来源（2026-07-30）。

## 完成定义

“完成课程”不是浏览全部页面，而是通过各环关口、跨项目迁移和最终综合项目。完成后应达到可靠的 LLM 应用、RAG、工作流和 Agent 工程水平，并具备继续进入预训练和架构研究所需的数学、模型与系统入口。

## 1. 数学表示与证据判断

能够区分现实对象、数学模型、符号表达、图表和计算结果；明确变量、参数、状态、输入、输出、单位、分母和时间范围；正确理解比例、增长率、分布、方差、置信区间和抽样偏差；区分相关、因果、模型置信、事件概率和行动风险；判断数据支持多强的结论。

## 2. 微积分、线性代数与张量计算

掌握导数、偏导数、梯度、雅可比和局部线性化；向量、基、矩阵、线性变换、投影、特征值和特征向量；张量形状、轴、转置、`reshape`、广播和批量矩阵乘法；点积、范数、归一化、余弦相似度和欧氏距离；秩、低维子空间、SVD 直觉和低秩更新。看到张量公式时能标出每个对象的形状、判断计算是否合法，并用 NumPy 或 PyTorch 核验。

## 3. 动力系统与物理结构

理解状态空间、微分方程、稳定性和反馈；复数旋转、相位、简谐振子和能量；耦合系统、特征模态和正常模态；离散系统到连续波、傅里叶分解和边界条件；标量场、向量场、梯度、散度、旋度和积分；变分、对称性与守恒。重点是掌握状态—演化、局部—整体、模态—叠加、边界—不变量等系统分析工具，而不是用类比替代证明。

## 4. 语言模型核心计算

能够解释并计算 token、词表、上下文、自回归生成、联合概率、条件概率、序列概率分解、logits、softmax、对数概率和下一 token 分布；能够比较温度、贪心、top-k 与 top-p；理解自信息、熵、交叉熵、KL 散度和困惑度；能解释为什么高概率不等于事实正确，低困惑度不等于应用成功。

## 5. Transformer 机制

掌握 Q、K、V 的来源和形状，解释 `QKᵀ`、除以 `√dₖ`、mask 与 softmax 维度以及权重怎样作用于 V；理解多头注意力、输出投影、前馈网络、残差、归一化、位置表示、因果掩码、上下文窗口和 KV cache；能够实现最小 Transformer 前向传播并排查形状错误。

## 6. 嵌入、检索与 RAG

能够解释嵌入检索有效与失效的条件；比较点积、余弦距离和欧氏距离；理解精确/近似最近邻、稠密/稀疏检索与重排；设计解析、切分、元数据、索引和版本追踪；计算 precision、recall、F1、Recall@k、MRR 和 nDCG；建立查询分层、相关性标注和 RAG 测试集；把错误分解到查询、切分、索引、召回、排序、上下文使用、生成和引用。

## 7. 优化、训练与轻量微调

理解计算图、链式法则、反向传播、梯度下降、mini-batch、学习率、动量和 Adam；区分训练、验证和测试，识别过拟合、数据泄漏并使用早停与正则化；区分训练损失与应用指标；理解 LoRA 的低秩更新、矩阵形状、rank 和参数量；能根据训练与验证曲线诊断欠拟合、过拟合和不稳定训练。

## 8. 数值精度、显存与计算排错

掌握浮点有限精度和非结合性，FP32、FP16、BF16 与量化整数的基本差别，上溢、下溢、NaN 与 Inf，稳定 softmax 与 log-sum-exp，混合精度与量化误差，以及参数、激活、梯度、优化器状态与显存占用的关系；能权衡精度、显存、延迟和吞吐。

## 9. API、工作流与 Agent 工程

能够设计请求—响应契约、JSON Schema、结构化输出、function calling 与工具参数验证；划分确定性步骤和模型判断；实现状态机、持久状态、超时、重试、幂等、补偿、恢复与人工复核；设计 Agent 循环、规划、路由、上下文和记忆；控制工具权限、终止条件与副作用；能选择单次调用、固定工作流或 Agent。

始终区分：

```text
结构合法 ≠ 内容正确
内容正确 ≠ 有权执行
工具调用成功 ≠ 现实结果正确
模型输出 ≠ 已授权行动
```

## 10. 多主体、规则和长期后果

理解策略、收益、最佳反应、均衡；网络结构、传播、瓶颈和级联；正/负反馈、延迟、阈值和路径依赖；机制设计、激励相容、参与约束和退出；社会选择、公平分配和决定权；多目标优化、Pareto 边界、鲁棒性和后悔值；排队、容量、调度和高峰拥塞；能够分析规则变化对用户、模型、运营者和受影响者的长期影响。

## 11. LLM 系统评估与可靠性

能够建立任务级、组件级和端到端评估；构造典型、边界、失败和对抗样本；校准人工标准与自动评分；测量成本、平均/P95 延迟、吞吐量和成功率；建立日志、trace、版本记录和错误分类；对模型、提示、语料、索引和工具执行回归测试；定义 SLO、上线门槛、停止条件和回滚方案。

## 最终综合项目

用户必须独立完成一个受控 LLM 工作流或 RAG Agent：

1. 从现实需求定义对象、目标、受影响者和失败边界；
2. 选择单次调用、工作流还是 Agent；
3. 设计结构化输出和工具接口；
4. 实现检索、状态、权限和副作用控制；
5. 建立组件级与端到端评估；
6. 测量质量、成本、延迟和可靠性；
7. 注入检索失败、超时、重复调用、错误参数等故障；
8. 解释系统为什么失败以及应修改哪一层；
9. 提供人工复核、停止、恢复和回滚路径；
10. 阅读相关论文与实现，定位公式、张量、目标函数、实验和假设。

## 明确边界

本课程为预训练和架构研究建立入口，但不能替代完整的机器学习、深度学习、矩阵微积分、数值分析、GPU 并行与分布式训练课程。
