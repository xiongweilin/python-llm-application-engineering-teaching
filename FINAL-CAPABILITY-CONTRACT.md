# 最终能力契约

状态：完整课程的唯一最终能力事实来源（2026-08-03）。

## 完成定义

“完成课程”不是浏览全部页面或复述术语，而是通过各环关口、跨项目迁移和最终综合项目。完成后应能独立设计、实现、评估和维护一个可靠、受控、可恢复的 LLM 工作流或 RAG Agent；能够在有限表示、有限维、可计算模型中形成足以参与高级 AI 系统判断的 minimum operational models，正式表达、计算、实现和验证不确定性下的决策、序贯策略、人机分工与多主体机制；并能把这些模型接入真实状态、权限、容量、评估、恢复和回滚边界。主线不宣称压缩一个完整数学或计算机科学学位；一般理论、连续/无限状态空间和复杂识别属于研究扩展。

## 主线建模原则

主线优先使用有限概率空间，有限状态、行动和时间范围，有限类型与有限行动博弈，有限容量和任务集合，有限维连续优化，以及能够枚举、求解、仿真或数值核验的模型。

始终明确：

```text
有限 ≠ 只做离散数学
有限维连续优化仍属于主线
会计算一个有限模型 ≠ 已掌握一般状态空间理论
仿真结果 ≠ 数学证明
数学最优行动 ≠ 组织允许执行
```

数学能力必须由定义、手算或推导、小数组实验、穷举、求解器、仿真、反例、失败诊断和条件变化后的重新求解来证明；自动检查、术语复述或同例照做不能单独形成能力记录。

教学调度使用分领域的 Minimum Viable Judgment，而不是一个全局阈值：`MVJ_Python`、`MVJ_Probability`、`MVJ_Optimization`、`MVJ_Sequential`、`MVJ_RAG` 和 `MVJ_Causal/Reliability` 分别由本领域的表示、假设、证据和行动链定义。某个领域跨过阈值，不会自动替其他领域撤掉基础脚手架。

## 1. 数学语言、概率、统计与证据

能够使用集合、映射、关系和量词表达对象与条件，区分必要条件、充分条件和等价条件；能够辨认定义与命题，完成有限或初等情形中的直接证明、反例和归纳，并说明假设与结论的边界。

能够建立有限概率空间和随机变量，计算联合、边缘和条件分布、Bayes 后验、期望、条件期望、方差和协方差；能够说明大数定律与中心极限定理的含义和适用条件；能够完成点估计、区间估计和基本假设检验，区分第一类与第二类错误；能够计算校准（calibration）、布里尔分数（Brier score）、覆盖率与选择回答条件下的风险，并直接应用霍夫丁界（Hoeffding bound）。给定数据或模型预测时，能够指出证据支持与不支持的结论，区分相关、因果、模型分数、事件概率和行动风险。

## 2. 线性代数与张量计算

能够使用向量、基、矩阵和线性变换表达对象，计算投影、特征值和特征向量；能够解释秩、低维子空间、SVD 和低秩更新。能够标注张量形状、轴、转置、`reshape`、广播和批量矩阵乘法，计算点积、范数、归一化、余弦相似度和欧氏距离；看到张量公式时能够判断计算是否合法，并用 NumPy 或 PyTorch 核验和定位形状、轴或广播错误。

## 3. 微积分、决策与约束优化

能够计算导数、偏导数、梯度、雅可比矩阵（Jacobian）和海森矩阵（Hessian），使用链式法则与局部线性化分析连续变化，并通过数值梯度检查验证实现。能够识别凸集和凸函数，建立有限维约束优化、线性规划和含整数变量的模型，使用拉格朗日（Lagrange）乘子与 KKT 条件分析候选解，并把网络流、匹配和多目标 Pareto 边界作为资源配置模型使用。

能够把未知状态、当前信念、可选行动、损失和信息获取写成单步决策模型，定义并计算有限单步模型中的信息价值，计算后验期望风险并选择自动执行、拒答、请求更多信息或人工升级。模型必须能够纳入不同错误损失、人工审核成本、审核容量、预算、权限以及至少一个公平或服务约束；能够枚举或使用求解器得到结果，解释起作用约束（binding constraint），并在成本、容量或约束变化后重新求解。

## 4. 状态演化、随机过程与序贯决策

能够建立离散时间状态演化和差分方程，计算固定点并分析基本稳定性、反馈、延迟、振荡和简单排队模型。能够判断给定状态表示和转移模型是否满足马尔可夫性质（Markov property），计算有限 Markov 链的转移矩阵、多步转移、可达关系、通信类、周期、平稳分布和吸收状态；能够判断不可约与非周期条件，并区分“存在或唯一的平稳分布”和“任意初始分布是否收敛到它”。

能够建立有限马尔可夫决策过程（MDP），定义策略、即时奖励、累计回报和价值函数，写出并计算 Bellman 期望方程与 Bellman 最优方程，使用策略迭代或价值迭代求解小模型；能够完成一个有限部分可观测马尔可夫决策过程（POMDP）的信念更新例子。能够解释探索—利用，计算累积后悔（cumulative regret），并用仿真比较固定策略、ε-贪心（ε-greedy）与上置信界（UCB）或汤普森采样（Thompson sampling）中至少一种策略。始终区分工作流状态、现实环境状态和 Agent 信念。

## 5. 语言模型核心计算

能够定义并计算 token、词表、上下文、自回归生成、联合概率、条件概率、序列概率分解、logits、softmax、对数概率和下一 token 分布；能够比较温度、贪心、top-k 与 top-p；能够计算自信息、熵、交叉熵、KL 散度和困惑度，并解释为什么高概率不等于事实正确、低困惑度不等于应用成功。

## 6. Transformer 机制

能够标注 Q、K、V 的来源和形状，计算 `QKᵀ`、除以 `√dₖ`、mask、softmax 和对 V 的加权；能够解释多头注意力、输出投影、前馈网络、残差、归一化、位置表示、因果掩码、上下文窗口和 KV cache 的计算责任；能够实现最小 Transformer 前向传播并修复形状、mask 或数值错误。

## 7. 嵌入、检索与 RAG

能够比较点积、余弦距离和欧氏距离，解释嵌入检索有效与失效的条件；能够比较精确与近似最近邻、稠密与稀疏检索及重排，设计解析、切分、元数据、索引和版本追踪；能够计算 precision、recall、F1、Recall@k、MRR 和 nDCG，建立查询分层、相关性标注和 RAG 测试集，并把错误分解到查询、切分、索引、召回、排序、上下文使用、生成和引用。

## 8. 优化训练、数值精度与计算排错

**主线核心：**能够建立计算图并使用链式法则和反向传播；能够运行基本梯度下降、小批量训练（mini-batch）、SGD、动量和 Adam；能够区分训练、验证和测试，识别过拟合与数据泄漏，使用基础早停和正则化，区分训练损失与应用指标，并对基础训练/验证曲线作出不过度承诺的判断。能够计算低秩适配（LoRA）的矩阵结构、秩（rank）和参数量，但不要求每位学习者实际开展系统性微调实验。

能够解释浮点有限精度和非结合性，诊断上溢、下溢、NaN 和 Inf，使用数值稳定的 softmax 与 log-sum-exp；能够对参数、激活、梯度和优化器状态作粗粒度资源估算，并权衡精度、内存、延迟和吞吐。

**按需扩展：**系统性 LoRA 实验、FP16/BF16 混合精度训练、量化误差的深入分析、精细显存分解、完整训练调参与曲线诊断，以及预训练和架构研究。接触这些内容不改变主线毕业关口；只有实际需求出现时才建立单独证据。

## 9. API、工作流、Agent 与人机合作

能够设计请求—响应契约、JSON Schema、结构化输出、function calling 和工具参数验证；能够划分确定性步骤与模型判断，实现状态机、持久状态、超时、重试、幂等、补偿、恢复、规划、路由、上下文、记忆和终止条件。

能够把环 5–8 的信念、单步决策和序贯策略接入真实权限与人工容量：使用既有校准或风险—覆盖率证据设定成本敏感阈值，在自动执行、请求更多信息、人工审核、拒绝和延迟处理之间作出可运行选择；把环 7 已验证的信息价值规则接入询问、工具调用和人工升级，不在本能力域重新教授其计算方法；能够建立专家加权或判断聚合，记录策略版本，并在置信分数失准、审核队列拥塞、人工判断噪声增加、工具权限撤销或成本函数变化时重新求解和安全降级。

始终区分：

```text
工作流状态 ≠ 环境状态 ≠ Agent 信念
结构合法 ≠ 内容正确
模型分数 ≠ 正确概率
内容正确 ≠ 有权执行
工具调用成功 ≠ 现实结果正确
模型输出 ≠ 已授权行动
```

## 10. 博弈、信息与有限机制分析

能够建立有限博弈，计算期望效用、最佳反应、纯策略与混合策略纳什均衡（Nash equilibrium），并分析重复互动中的基本激励；能够建立有限贝叶斯博弈（Bayesian game），计算贝叶斯—纳什均衡（Bayes–Nash equilibrium）。能够表达社会选择和直接机制，在有限类型与有限报告集合上列出分配和支付规则，穷举各类型效用，检查占优策略激励相容（DSIC）或贝叶斯激励相容（BIC）、个体理性（IR）、效率和预算平衡，找出操纵路径或给出有限范围内不存在操纵的验证。

能够实现并比较一个有限拍卖、任务分配或稳定匹配例子；使用 VCG 时能够验证具体实例而不把它扩张为对完整理论族的掌握；讨论稳定匹配的策略性质时必须明确哪一方、哪个机制和哪些假设。能够分析评分、奖励、路由或人工审核制度中的参与、退出、公平和受影响者后果。

## 11. 实验、因果评估与系统可靠性

能够分别回答描述性评估“发生了什么”、统计推断“差异是否可能来自抽样波动”和因果评估“系统改动是否造成结果变化”。能够定义估计目标（estimand），设计随机分配，计算和解释平均处理效应（ATE），进行基本样本量与统计功效分析，识别多重比较和序贯查看偏差。

能够使用潜在结果和有限因果有向无环图（causal DAG）表达问题，识别因果路径、背门路径、碰撞点（collider）及其后代，使用有限图上的 d-分离（d-separation）与背门准则判断候选调整集；在小型 DAG 中能够找出开放的背门路径、识别不应控制的碰撞点，并说明结论依赖的图结构假设。能够识别混杂、选择偏差、策略产生的数据选择偏差、识别假设和无法识别的部分。

能够建立任务级、组件级和端到端评估，构造典型、边界、失败和对抗样本，校准人工标准与自动评分；能够测量质量、成本、平均与 P95 延迟、吞吐量和成功率，建立日志、trace、版本记录、错误分类和回归测试，定义 SLO、上线门槛、停止条件与回滚方案，并依据证据作出上线、停止或继续实验的决定。

## 最终综合项目

用户必须独立完成一个受控 LLM 工作流或 RAG Agent，把新增数学能力连接到同一个现实问题，而不是完成一组彼此无关的附加作业。每一项理论工具都必须先做适用性判断：现实对象、假设和证据支持使用它时才建模；如果不适用，必须说明为什么不适用，并用一个邻近的真实子问题完成相应能力证据，而不是为了 rubric 人为塞入模型：

```text
现实目标与受影响者
→ 损失函数和权限边界
→ 自动、拒答、询问或升级策略
→ 容量与序贯反馈
→ 主体激励和操纵空间
→ 实验或因果评估
→ 上线、停止、恢复和回滚
```

最终关口要求六组相互连接的证据。每组先提交“适用 / 不适用 / 尚不确定”的判断及理由：

1. **决策证据**：定义现实对象、状态、行动、损失、受影响者、容量、权限与失败边界。
2. **策略证据**：选择单次调用、工作流或 Agent，设计结构化输出、工具和检索接口，并使用 calibration 或风险—覆盖率证据选择自动化、询问、拒答与升级阈值。
3. **序贯证据**：构造并求解一个与项目决策相关的有限 MDP、排队模型或 bandit 仿真，解释策略变化和适用边界。
4. **机制证据**：分析至少两个主体的效用、报告、操纵、参与、退出、效率或公平后果，并实现一个有限分配、支付或匹配规则。
5. **因果证据**：给出 estimand、识别假设、实验或观察性方案，区分描述、统计和因果结论，并标出无法识别的部分。
6. **工程证据**：把上述策略接入持久状态、权限、日志、trace 和策略版本；测量质量、成本、延迟与可靠性；注入检索失败、超时、重复调用、错误参数、失准、拥塞或权限撤销；提供人工复核、停止、恢复和回滚路径，并能在相关论文或实现中定位公式、张量、目标、实验和假设。

序贯决策与机制证据可以通过与同一现实问题对应的离线有限模型、仿真或回放分析完成；不要求将强化学习、拍卖、支付机制或复杂匹配部署到生产系统。离线模型必须与项目的真实对象、数据、损失、容量和权限边界相连。只有实际接入运行系统的部分才承担持久状态、权限、故障注入和回滚要求。

## 研究扩展范围

以下内容不是主线毕业关口：完整实分析与测度概率、不可数样本空间、一般或连续状态空间随机过程、一般 POMDP 求解、函数空间优化、系统性 LoRA 实验、FP16/BF16 混合精度训练、量化误差深入分析、精细显存分解、完整训练调参与曲线诊断、一般 Bayesian revelation principle、Myerson 最优拍卖、多维类型与动态机制设计、机制计算复杂度与近似、高维统计推断、复杂因果识别、完整 do-calculus、复杂工具变量理论、一般动态处理制度、数学物理与连续场、完整大模型预训练、架构研究、GPU 内核和分布式训练。

当问题确实进入连续或无限状态空间、一般收敛与可测性、一般 Bayesian mechanism、高维统计推断或复杂因果识别时，再进入相应研究扩展；主线中的有限计算、数值核验或仿真不得被描述为已经掌握这些一般理论。
