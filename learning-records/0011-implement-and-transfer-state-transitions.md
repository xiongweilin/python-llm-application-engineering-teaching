# 实现并迁移受限制的状态转换

用户能根据“当前状态 + 事件”预测允许路径和拒绝路径，独立用二元组键与字典实现状态转换，并对未定义组合抛出 `ValueError`；还能在新增 `waiting + cancel → cancelled` 后保持 `running + cancel` 被拒绝。用户能说明稳定结构是“建键 → 查表 → 非法则抛异常 → 合法则返回”，变化部分是转换表数据与接口约定，而 Python 对象模型没有因此改变。这证明正式会话 1“状态与允许行动”已经通过，可以进入失败分类、有限重试、等待间隔与停止条件的实现。
