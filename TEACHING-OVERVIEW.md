# Python、数学、LLM、RAG 与 Agent 工程课程总览

## 当前状态（2026-07-31）

- 已有 11 条学习记录；最新记录证明用户能预测、实现并迁移受限制的状态转换。
- 当前位于**阶段 A · 环 1 · 正式会话 2“重试、防重复与批处理”**。
- 10 条已有记录已经整合为综合复习 R1；它不改变正式进度，也不自动产生新记录。
- 当前环 1 由三个正式会话组成，只是完整课程的第一个 Python 环，不是整门课程的大部分。

入口：[课程首页](index.html) · [当前正式会话](lessons/0012-failure-retry-stop.html) · [已完成会话 1](lessons/0010-state-and-permitted-actions.html) · [综合复习](lessons/0011-integrated-review-established-capabilities.html) · [完整课程路线](reference/0002-course-progress.html) · [最终能力契约](reference/0005-final-capabilities.html) · [学习会话规范](reference/0004-session-page-contract.html)

## 唯一最终目标

用户最终能够独立设计、实现、评估和维护一个受控的 LLM 工作流或 RAG Agent；理解关键数学、张量、模型和系统机制；控制状态、权限与现实副作用；用测试、指标、trace、故障注入、恢复和回滚证明行为；并能阅读论文与实现中的公式、张量、目标函数、实验和假设。

完整定义只由 [FINAL-CAPABILITY-CONTRACT.md](FINAL-CAPABILITY-CONTRACT.md) 持有。其他页面只说明自己如何服务于该目标，不能重新定义或缩小它。

## 为什么采用四阶段、十七环

原先七环路线适合作为可靠应用工程骨架，却不足以承担完整的数学、物理、Transformer、训练数值和多主体能力。整改后的结构如下：

1. **阶段 A：Python 与真实系统代码所有权**（环 0–4）
   - 已有判断、可执行 Python、Python 工程基础、Catalog、Feedback。
2. **阶段 B：数学、张量与系统分析基础**（环 5–9）
   - 数学证据、线性代数与张量、微积分与优化、动力系统与物理、多主体与长期后果。
3. **阶段 C：语言模型、Transformer、训练与数值机制**（环 10–12）
   - 语言模型核心计算、Transformer、优化训练/LoRA/数值精度与显存。
4. **阶段 D：RAG、工作流、Agent 与可靠交付**（环 13–16）
   - 检索与 RAG、API/工作流/Agent、系统评估与可靠性、最终综合项目与论文阅读。

完整的环级底层逻辑、编程落点和关口只由[完整课程路线](reference/0002-course-progress.html)持有，避免在多个文件中产生不同版本。

## 项目与理论怎样交织

课程不采用“先学完全部数学再碰项目”，也不采用“项目暂时没用就删除理论”。采用双向螺旋：

```text
真实项目暴露问题
  ↓
抽取底层结构与所需数学
  ↓
用最小计算和代码建立机制
  ↓
回到项目实现、测试和诊断
  ↓
迁移到另一个项目或陌生情境
```

- 项目可以提前触发后续数学或模型知识。
- 每个数学、物理和模型能力域仍有独立关口，不能因为项目未触发而消失。
- Catalog 与 Feedback 始终作为两次实现和迁移场景；最终综合项目接入其中一个真实边界。

## 从架构到节点的固定观察层

```text
架构：现实结果、数据、决定权和副作用怎样流动
  ↓
模块：哪个模块拥有入口、流程、规则、状态和适配责任
  ↓
节点：哪个函数、类型、查询、张量或异常路径实现责任
  ↓
验证：测试、计算核验、运行证据、指标和故障注入
  ↓
迁移：改变条件、项目、数据或模型后重新判断
```

## 每个正式会话的固定节奏

```text
会话地图：说明当前位置、三个不同问题及其责任边界
→ 问题一：定向 → 底层逻辑 → 具体细节 → 预测 → 核心实现 → 迁移 → 小结
→ 问题二：定向 → 底层逻辑 → 具体细节 → 预测 → 核心实现 → 迁移 → 小结
→ 问题三：定向 → 底层逻辑 → 具体细节 → 预测 → 核心实现 → 迁移 → 小结
→ 会话汇总：一次提交九项任务证据、三个问题小结和整体边界
```

- 一个问题建立一个中心能力，并包含预测、核心实现和迁移三个任务；通常三个问题组成一个 30–45 分钟正式会话，因此三个问题共九项任务。
- 每 2–3 个正式会话安排一次无答案间隔回忆。
- 提示从解释代码逐步退到独立定位和设计。
- 每页下方有本会话中文术语；跨会话总术语表只用于索引。
- 自动检查不能单独产生学习记录。

## 证据与晋级

证据分成接触、同例调用、变式调用和跨情境迁移。只有后两层能够推进能力关口。

- Python 能力需要代码修改、测试和失败诊断。
- 数学与张量能力需要手算、小数组实验、形状或梯度核验。
- 模型能力需要最小实现、机制解释和故意错误排查。
- 系统能力需要状态、权限、失败、恢复、测量和现实结果证据。
- 最终项目由用户主导需求、架构、实现、评估、故障注入、回滚和答辩。

## 课程生成原则

完整路线约为 90–120 个正式会话的规划量级，但不一次性生成。每次只建立当前位置附近的 1–3 个会话，根据用户表现调整支架和数量。估计数量可以变化，最终能力域与通过关口不能静默删除。
